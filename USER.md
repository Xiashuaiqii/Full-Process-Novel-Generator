# TomatoNovel - 番茄小说创作助手

一个基于 DeepSeek AI 的本地小说创作工具，帮助作者管理小说结构、生成章节内容、维护知识库一致性。

## 功能特性

### 1. 小说项目管理
- 创建和管理多个小说项目
- 设置小说类型、主题、简介、写作风格、目标读者
- 支持归档功能

### 2. 卷/章节结构
- 支持「卷 → 章」两级结构
- 章节大纲编写
- 章节正文编辑与保存
- 章节历史版本管理

### 3. AI 辅助写作（DeepSeek）
- **章节生成**：根据大纲、前文、知识库自动生成章节正文
- **章节摘要**：自动生成长/短摘要
- **知识抽取**：从章节内容自动提取角色、地点、势力等信息
- **一致性检查**：检查内容是否与知识库冲突

### 4. 知识库管理
支持以下实体类型：
- **固定设定/世界观**：世界观、修炼体系、规则、历史等
- **角色**：姓名、别名、性别、年龄、身份、性格、外貌、动机、秘密、境界、能力等
- **地点地图**：世界、大陆、国家、城市、门派、秘境等层级结构
- **势力**：宗门、家族、组织等
- **角色关系**：关系类型、关系强度、当前状态
- **伏笔**：埋下/揭露状态、重要性分级
- **物品**：法宝、丹药、秘籍等
- **时间线事件**：故事时间轴管理

### 5. 知识库更新审核
- AI 只提出建议，不直接写入正式知识库
- 所有更新需作者审核后确认
- 支持接受/拒绝/编辑后接受三种操作

### 6. 日志中心
- 应用日志查看
- AI 调用日志（Prompt/Response/Usage）
- 支持按级别、范围、小说、章节筛选

### 7. DeepSeek 设置
- API Key 管理（本地加密存储）
- 任务级参数配置：
  - 章节正文生成
  - 章节摘要生成
  - 知识库更新抽取
  - 一致性检查
- 可配置项：模型选择、思考模式、温度、最大 Token、流式输出等

## 技术栈

- **框架**：Next.js 15 + React 19
- **语言**：TypeScript
- **样式**：Tailwind CSS
- **数据库**：SQLite + Prisma ORM
- **AI**：DeepSeek API (OpenAI SDK)
- **状态管理**：Zustand + TanStack Query
- **UI 组件**：shadcn/ui

## 启动方式

### 1. 安装依赖
```bash
npm install
```

### 2. 初始化配置
```bash
npm run setup
```
此命令会：
- 自动创建 `.env` 文件（如果不存在）
- 生成随机的 `ENCRYPTION_KEY`
- 生成 Prisma Client
- 推送数据库结构
- 执行种子数据

### 3. 启动开发服务器
```bash
npm run dev
```
应用将在 http://localhost:3000 启动

### 4. 配置 DeepSeek
1. 访问设置页面（http://localhost:3000/settings）
2. 输入 DeepSeek API Key
3. 测试连接
4. 根据需要调整各任务的参数

## 环境变量

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `DATABASE_URL` | SQLite 数据库路径 | `file:./dev.db` |
| `ENCRYPTION_KEY` | API Key 加密密钥 | 自动生成 |
| `NEXT_PUBLIC_APP_URL` | 应用 URL | `http://localhost:3000` |

## 常用命令

```bash
# 开发模式
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm run start

# 数据库操作
npm run prisma:generate    # 生成 Prisma Client
npm run prisma:migrate     # 执行数据库迁移
npm run prisma:studio      # 打开 Prisma Studio
npm run db:push            # 推送结构变更
npm run db:seed            # 执行种子数据

# 一键初始化
npm run setup
```

## 使用流程

1. **创建小说** → 首页点击「新建小说」
2. **创建卷/章节** → 进入小说工作台，添加卷和章节
3. **编写大纲** → 在章节编辑区填写大纲
4. **配置知识库** → 添加世界观、角色、地点等基础设定
5. **生成章节** → 选择上下文范围，点击生成
6. **审核知识更新** → 查看 AI 建议的知识库更新，审核确认
7. **继续下一章** → 重复步骤 3-6

## 注意事项

- 所有数据存储在本地 SQLite 数据库中
- API Key 使用本地密钥加密存储
- 建议定期备份 `prisma/dev.db` 文件
